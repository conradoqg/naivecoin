{
  "name": "naivecoin",
  "version": "1.0.0",
  "description": "Naivecoin, a cryptocurrency demonstration",
  "homepage": "https://github.com/conradoqg/naivecoin",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/conradoqg/naivecoin.git"
  },
  "bin": {
    "naivecoin": "bin/naivecoin.js"
  },
  "scripts": {
    "start": "cross-env NODE_ENV=development node bin//naivecoin.js",
    "compose:up": "docker-compose up",
    "coveralls": "cross-env NODE_ENV=test nyc npm test && nyc report --reporter=text-lcov | coveralls",
    "coverage": "cross-env NODE_ENV=test LOG_LEVEL=0 nyc --reporter=lcov --reporter=text --clean node node_modules/mocha/bin/_mocha -u bdd --recursive --colors test/",
    "test": "eslint --fix bin lib test && cross-env NODE_ENV=test LOG_LEVEL=0 mocha -u bdd --recursive --colors test/",
    "diagrams": "mmdc -i doc/blockchain.mmd -o doc/blockchain.png -b transparent && mmdc -i doc/components.mmd -o doc/components.png -b transparent",
    "lint": "eslint --fix bin lib test"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "chalk": "^2.4.1",
    "elliptic": "^6.4.0",
    "es6-error": "^4.0.2",
    "express": "^4.16.2",
    "fs-extra": "^6.0.1",
    "pug": "^2.0.0-rc.4",
    "ramda": "^0.25.0",
    "statuses": "^1.3.1",
    "superagent": "^3.5.2",
    "swagger-ui-express": "^1.0.8",
    "threads": "^0.12.0",
    "timeago.js": "^3.0.2",
    "yargs": "^11.0.0"
  },
  "engines": {
    "node": ">=8"
  },
  "devDependencies": {
    "coveralls": "^3.0.0",
    "cross-env": "^5.1.6",
    "eslint": "^4.19.1",
    "mermaid.cli": "^0.3.1",
    "mocha": "^3.3.0",
    "mocha-steps": "^1.0.2",
    "nyc": "^10.3.0",
    "supertest": "^1.0.0"
  }
}
